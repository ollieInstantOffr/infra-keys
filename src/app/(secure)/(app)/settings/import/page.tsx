import { ImportExportSettings } from "@/components/settings/import-export";

export const metadata = { title: "Import / export" };

export default function ImportPage() {
  return <ImportExportSettings />;
}
